{
  "name": "clipshare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy --env production",
    "migrate:local": "wrangler d1 migrations apply clipshare --local",
    "migrate:prod": "wrangler d1 migrations apply clipshare --env production --remote"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.60",
    "@cloudflare/workers-types": "^4.20250617.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.21.0"
  }
}
